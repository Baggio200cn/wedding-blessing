# 生产环境依赖 - 针对Vercel优化
requests==2.31.0
python-dateutil==2.8.2

# 如果需要数据处理功能，添加轻量级库
# numpy==1.24.3
# pandas==2.0.3

# 注意：避免使用以下重型库，它们可能导致Vercel部署失败
# tensorflow>=2.0.0  # 过大，不适合Serverless
# torch>=1.0.0       # 过大，不适合Serverless
# scikit-learn>=1.0  # 如需要，使用轻量级替代方案

# 如果确实需要机器学习功能，可以考虑：
# 1. 使用云端ML API（如Claude API、OpenAI API）
# 2. 使用轻量级库如：
#    - lightgbm==3.3.5  # 比XGBoost更轻量
#    - joblib==1.3.2    # 用于模型序列化
# 3. 或者将ML模型部署到专门的服务上，通过API调用
